# Linux commands
alias ls="ls --color"
alias ll="ls -ltr"
alias la="ls -a"

alias vi="vim"

alias mkdir="mkdir -p"

alias cd="pushd > /dev/null"

#pushd() {
#  if [ $# -eq 0 ]; then
#    DIR="$HOME"
#  else
#    DIR="$1"
#  fi
#
#  builtin pushd "$DIR" > /dev/null
#  echo -n "DIRSTACK: "
#  dirs
#}
# User commands
alias g="git"

alias b="bundle"
alias be="b exec"

alias rs="redscript"

alias ca="casperjs"
