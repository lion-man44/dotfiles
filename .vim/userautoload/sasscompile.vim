" 自動コンパイル実行する拡張子
let g:sass_compile_file = ['sass', 'scss']

" cssが入っているディレクトリ名
let g:sass_compile_cssdir = ['css', 'sytlesheet']

" コンパイル実行前に実行するコマンド
let g:sass_compile_beforecmd = ""

" コンパイル実行後に実行するコマンド
"let g:sass_compile_aftercmd = ""

" ファイル保存時に自動コンパイル
let g:sass_compile_auto = 1
